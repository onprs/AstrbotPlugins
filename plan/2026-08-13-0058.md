# @grok 群消息响应插件

状态：已完成

## 目标

开发新插件 `plugins/astrbot_plugin_grok_responder` 并部署上线：群聊中任何包含 `@grok`（不区分大小写）的消息触发响应，回复由 AstrBot 当前模型（LLM）生成，不 @ 发送者。

## 任务

- [x] 确认"做出反应"的具体形式：LLM 生成回复（方案 B），不 @ 发送者，必须含 `@` 符号才触发。
- [x] 身份复用：不写死 Grok 身份，LLM 请求自动复用 AstrBot 已配置的 persona 与 prompt_prefix，仅追加输出约束。
- [x] 编写插件 main.py / metadata.yaml / README.md / _conf_schema.json。
- [x] Ruff 检查 + py_compile 本地通过，16 项单元测试全部通过。
- [x] 同步到 `astrbot_beijing`，重启 AstrBot。
- [x] 检查插件加载、服务日志，并验证触发行为。

## 验证

- [x] 线上插件加载成功：`Plugin astrbot_plugin_grok_responder (1.0.0) by onprs`，OneBot v11 适配器重连正常。
- [x] 修复 metadata.yaml YAML 解析错误（`display_name` 以 `@` 开头需加引号）。
- [x] 端到端测试（伪 OneBot universal 客户端连接反向 WS）：发含 `@grok` 群消息 → 插件触发 → LLM 生成回复 → 仅 1 次 `send_group_msg`（内容"收到~测试成功触发啦（眨眼）"，身份 prompt 生效），无重复回复（stop_event 生效）。
- [x] 负例：发不带 `@` 的 `grok` 消息 → 无任何回复，日志确认未触发。
- [x] 测试后数据库无残留（stop_event 阻止主流程，未创建会话记录）；临时脚本已清理。
- [x] 线上模型链路正常（mimo-v2.5 生成回复成功）。
