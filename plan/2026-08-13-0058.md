# @grok 群消息响应插件

状态：已完成

## 目标

开发新插件 `plugins/astrbot_plugin_grok_responder` 并部署上线：群聊中任何包含 `@grok`（不区分大小写）的消息触发 AstrBot 主流程回复，插件仅承担触发作用，完整共享会话上下文。

## 任务

- [x] 确认需求：由 AstrBot 主流程处理回复，插件只做触发（v2 改造）。
- [x] v2 实现：命中 @grok 后设置 `event.is_at_or_wake_command=True` 标记唤醒，不调用 LLM、不产生结果；主流程自动发起 LLM 请求。
- [x] 更新配置 schema（移除 fallback_text）、README、metadata（v1.1.0）、单元测试（14 项）+ Ruff 通过。
- [x] 同步到 `astrbot_beijing`，重启 AstrBot。
- [x] 检查插件加载、服务日志，端到端验证触发、上下文共享与负例。

## 验证

- [x] 线上插件加载成功：`Plugin astrbot_plugin_grok_responder (1.1.0) by onprs`。
- [x] 端到端正例（伪 OneBot universal 客户端）：`@grok 测试上下文：记住暗号 蓝鲸` → 主流程回复"记住了喵，蓝鲸是吧～"；等待越过冷却后第二条 `@grok 我刚才告诉你的暗号是什么？` → 回复"暗号是蓝鲸喵～"——**上下文共享确认**（第二条请求的 messages 完整携带第一条对话历史）。
- [x] 主流程请求日志确认：persona 身份（OnprsAssistant）、Safe Mode、Skills、workspace、Group Reply Safety（group_reply_guard）全部按标准流程生效。
- [x] 插件日志：两次 `GrokResponder: 群 1919810 命中 @grok，已唤醒主流程处理`。
- [x] 负例：`grok 不带艾特测试` → 无任何回复。
- [x] 冷却生效：20 秒内第二条未触发（默认 15 秒），越过冷却后正常。
- [x] 测试数据清理：conversations 中测试会话已删除（integrity ok），无残留工作区；临时脚本已清理。
